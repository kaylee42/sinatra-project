<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Users:</h1>
    <ul>
      <% users = User.all.sort_by {|user| user.username}%>
      <% users.each do |user| %>
      <li><a href="/user/<%=user.id%>"><%=user.username%></a></li>
      <% end %>
    </ul>

  </body>
</html>
